import 'package:flutter/material.dart';

class JuegosMenu extends StatelessWidget {
  const JuegosMenu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}